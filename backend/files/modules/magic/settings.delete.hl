.arguments
   name:string
auth.ticket.verify:admin
add:x:./*/wait.signal/*/ids
   get-nodes:x:@.arguments/*
wait.signal:magic.db.mysql.delete
   database:magic_publishing
   table:settings
   ids
magic.publishing.settings.flush
return-nodes:x:@wait.signal/*
