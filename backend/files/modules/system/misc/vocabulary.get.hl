
/*
 * Returns all slots registered in the system.
 */
.description:"Returns all slots registered in the system."


/*
 * Ensures user is authorized to access endpoint.
 */
auth.ticket.verify:admin, editor, author


/*
 * Retrieving all system slots.
 */
vocabulary


/*
 * Returning results of the above to caller.
 */
return-nodes:x:@vocabulary/*